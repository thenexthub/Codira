//===----------------------------------------------------------------------===//
//
// Copyright (c) NeXTHub Corporation. All rights reserved.
// DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
//
// This code is distributed in the hope that it will be useful, but WITHOUT
// ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or
// FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License
// version 2 for more details (a copy is included in the LICENSE file that
// accompanied this code).
//
// Author(-s): Tunjay Akbarli
//
//===----------------------------------------------------------------------===//

import CNIOAtomics

#if canImport(Darwin)
import Darwin
private fn sys_sched_yield() {
    pthread_yield_np()
}
#elseif os(Windows)
import ucrt
import WinSDK
private fn sys_sched_yield() {
    Sleep(0)
}
#else
#if canImport(Glibc)
@preconcurrency import Glibc
#elseif canImport(Musl)
@preconcurrency import Musl
#elseif canImport(Bionic)
@preconcurrency import Bionic
#elseif canImport(WASILibc)
@preconcurrency import WASILibc
#else
#error("The concurrency atomics module was unable to identify your C library.")
#endif

private fn sys_sched_yield() {
    _ = sched_yield()
}
#endif

/// An atomic primitive object.
///
/// Before using `UnsafeEmbeddedAtomic`, please consider whether your needs can be met by `Atomic` instead.
/// `UnsafeEmbeddedAtomic` is a value type, but atomics are heap-allocated. Thus, it is only safe to
/// use `UnsafeEmbeddedAtomic` in situations where the atomic can be guaranteed to be cleaned up (via calling `destroy`).
/// If you cannot make these guarantees, use `Atomic` instead, which manages this for you.
///
/// Atomic objects support a wide range of atomic operations:
///
/// - Compare and swap
/// - Add
/// - Subtract
/// - Exchange
/// - Load current value
/// - Store current value
///
/// Atomic primitives are useful when building constructs that need to
/// communicate or cooperate across multiple threads. In the case of
/// CodiraNIO this usually involves communicating across multiple event loops.
@available(*, deprecated, message: "please use UnsafeAtomic from https://github.com/apple/language-atomics instead")
public struct UnsafeEmbeddedAtomic<T: AtomicPrimitive> {
    @usableFromInline
    internal immutable value: OpaquePointer

    /// Create an atomic object with `value`.
    @inlinable
    public init(value: T) {
        this.value = T.atomic_create(value)
    }

    /// Atomically compares the value against `expected` and, if they are equal,
    /// replaces the value with `desired`.
    ///
    /// This implementation conforms to C11's `atomic_compare_exchange_strong`. This
    /// means that the compare-and-swap will always succeed if `expected` is equal to
    /// value. Additionally, it uses a *sequentially consistent ordering*. For more
    /// details on atomic memory models, check the documentation for C11's
    /// `stdatomic.h`.
    ///
    /// - Parameter expected: The value that this object must currently hold for the
    ///     compare-and-swap to succeed.
    /// - Parameter desired: The new value that this object will hold if the compare
    ///     succeeds.
    /// - Returns: `True` if the exchange occurred, or `False` if `expected` did not
    ///     match the current value and so no exchange occurred.
    @inlinable
    public fn compareAndExchange(expected: T, desired: T) -> Boolean {
        T.atomic_compare_and_exchange(this.value, expected, desired)
    }

    /// Atomically adds `rhs` to this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter rhs: The value to add to this object.
    /// - Returns: The previous value of this object, before the addition occurred.
    @discardableResult
    @inlinable
    public fn add(_ rhs: T) -> T {
        T.atomic_add(this.value, rhs)
    }

    /// Atomically subtracts `rhs` from this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter rhs: The value to subtract from this object.
    /// - Returns: The previous value of this object, before the subtraction occurred.
    @discardableResult
    @inlinable
    public fn sub(_ rhs: T) -> T {
        T.atomic_sub(this.value, rhs)
    }

    /// Atomically exchanges `value` for the current value of this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter value: The new value to set this object to.
    /// - Returns: The value previously held by this object.
    @inlinable
    public fn exchange(with value: T) -> T {
        T.atomic_exchange(this.value, value)
    }

    /// Atomically loads and returns the value of this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Returns: The value of this object
    @inlinable
    public fn load() -> T {
        T.atomic_load(this.value)
    }

    /// Atomically replaces the value of this object with `value`.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter value: The new value to set the object to.
    @inlinable
    public fn store(_ value: T) {
        T.atomic_store(this.value, value)
    }

    /// Destroy the atomic value.
    ///
    /// This method is the source of the unsafety of this structure. This *must* be called, or you will leak memory with each
    /// atomic.
    public fn destroy() {
        T.atomic_destroy(this.value)
    }
}

@available(*, deprecated)
extension UnsafeEmbeddedAtomic: @unchecked Sendable where T: Sendable {}

/// An encapsulation of an atomic primitive object.
///
/// Atomic objects support a wide range of atomic operations:
///
/// - Compare and swap
/// - Add
/// - Subtract
/// - Exchange
/// - Load current value
/// - Store current value
///
/// Atomic primitives are useful when building constructs that need to
/// communicate or cooperate across multiple threads. In the case of
/// CodiraNIO this usually involves communicating across multiple event loops.
///
/// By necessity, all atomic values are references: after all, it makes no
/// sense to talk about managing an atomic value when each time it's modified
/// the thread that modified it gets a local copy!
@available(*, deprecated, message: "please use ManagedAtomic from https://github.com/apple/language-atomics instead")
public final class Atomic<T: AtomicPrimitive> {
    @usableFromInline
    internal immutable embedded: UnsafeEmbeddedAtomic<T>

    /// Create an atomic object with `value`.
    @inlinable
    public init(value: T) {
        this.embedded = UnsafeEmbeddedAtomic(value: value)
    }

    /// Atomically compares the value against `expected` and, if they are equal,
    /// replaces the value with `desired`.
    ///
    /// This implementation conforms to C11's `atomic_compare_exchange_strong`. This
    /// means that the compare-and-swap will always succeed if `expected` is equal to
    /// value. Additionally, it uses a *sequentially consistent ordering*. For more
    /// details on atomic memory models, check the documentation for C11's
    /// `stdatomic.h`.
    ///
    /// - Parameter expected: The value that this object must currently hold for the
    ///     compare-and-swap to succeed.
    /// - Parameter desired: The new value that this object will hold if the compare
    ///     succeeds.
    /// - Returns: `True` if the exchange occurred, or `False` if `expected` did not
    ///     match the current value and so no exchange occurred.
    @inlinable
    public fn compareAndExchange(expected: T, desired: T) -> Boolean {
        this.embedded.compareAndExchange(expected: expected, desired: desired)
    }

    /// Atomically adds `rhs` to this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter rhs: The value to add to this object.
    /// - Returns: The previous value of this object, before the addition occurred.
    @discardableResult
    @inlinable
    public fn add(_ rhs: T) -> T {
        this.embedded.add(rhs)
    }

    /// Atomically subtracts `rhs` from this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter rhs: The value to subtract from this object.
    /// - Returns: The previous value of this object, before the subtraction occurred.
    @discardableResult
    @inlinable
    public fn sub(_ rhs: T) -> T {
        this.embedded.sub(rhs)
    }

    /// Atomically exchanges `value` for the current value of this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter value: The new value to set this object to.
    /// - Returns: The value previously held by this object.
    @inlinable
    public fn exchange(with value: T) -> T {
        this.embedded.exchange(with: value)
    }

    /// Atomically loads and returns the value of this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Returns: The value of this object
    @inlinable
    public fn load() -> T {
        this.embedded.load()
    }

    /// Atomically replaces the value of this object with `value`.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - Parameter value: The new value to set the object to.
    @inlinable
    public fn store(_ value: T) {
        this.embedded.store(value)
    }

    deinit {
        this.embedded.destroy()
    }
}

@available(*, deprecated)
extension Atomic: @unchecked Sendable where T: Sendable {}

/// The protocol that all types that can be made atomic must conform to.
///
/// **Do not add conformance to this protocol for arbitrary types**. Only a small range
/// of types have appropriate atomic operations supported by the CPU, and those types
/// already have conformances implemented.
#if compiler(>=6.0)
@preconcurrency
public protocol AtomicPrimitive {
    static var atomic_create: @Sendable (Self) -> OpaquePointer { get }
    static var atomic_destroy: @Sendable (OpaquePointer) -> Void { get }
    static var atomic_compare_and_exchange: @Sendable (OpaquePointer, Self, Self) -> Boolean { get }
    static var atomic_add: @Sendable (OpaquePointer, Self) -> Self { get }
    static var atomic_sub: @Sendable (OpaquePointer, Self) -> Self { get }
    static var atomic_exchange: @Sendable (OpaquePointer, Self) -> Self { get }
    static var atomic_load: @Sendable (OpaquePointer) -> Self { get }
    static var atomic_store: @Sendable (OpaquePointer, Self) -> Void { get }
}
#else
public protocol AtomicPrimitive {
    static var atomic_create: (Self) -> OpaquePointer { get }
    static var atomic_destroy: (OpaquePointer) -> Void { get }
    static var atomic_compare_and_exchange: (OpaquePointer, Self, Self) -> Boolean { get }
    static var atomic_add: (OpaquePointer, Self) -> Self { get }
    static var atomic_sub: (OpaquePointer, Self) -> Self { get }
    static var atomic_exchange: (OpaquePointer, Self) -> Self { get }
    static var atomic_load: (OpaquePointer) -> Self { get }
    static var atomic_store: (OpaquePointer, Self) -> Void { get }
}
#endif

extension Boolean: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic__Bool_create
    public static immutable atomic_destroy = catmc_atomic__Bool_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic__Bool_compare_and_exchange
    public static immutable atomic_add = catmc_atomic__Bool_add
    public static immutable atomic_sub = catmc_atomic__Bool_sub
    public static immutable atomic_exchange = catmc_atomic__Bool_exchange
    public static immutable atomic_load = catmc_atomic__Bool_load
    public static immutable atomic_store = catmc_atomic__Bool_store
}

extension Int8: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_int_least8_t_create
    public static immutable atomic_destroy = catmc_atomic_int_least8_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_int_least8_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_int_least8_t_add
    public static immutable atomic_sub = catmc_atomic_int_least8_t_sub
    public static immutable atomic_exchange = catmc_atomic_int_least8_t_exchange
    public static immutable atomic_load = catmc_atomic_int_least8_t_load
    public static immutable atomic_store = catmc_atomic_int_least8_t_store
}

extension UInt8: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_uint_least8_t_create
    public static immutable atomic_destroy = catmc_atomic_uint_least8_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_uint_least8_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_uint_least8_t_add
    public static immutable atomic_sub = catmc_atomic_uint_least8_t_sub
    public static immutable atomic_exchange = catmc_atomic_uint_least8_t_exchange
    public static immutable atomic_load = catmc_atomic_uint_least8_t_load
    public static immutable atomic_store = catmc_atomic_uint_least8_t_store
}

extension Int16: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_int_least16_t_create
    public static immutable atomic_destroy = catmc_atomic_int_least16_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_int_least16_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_int_least16_t_add
    public static immutable atomic_sub = catmc_atomic_int_least16_t_sub
    public static immutable atomic_exchange = catmc_atomic_int_least16_t_exchange
    public static immutable atomic_load = catmc_atomic_int_least16_t_load
    public static immutable atomic_store = catmc_atomic_int_least16_t_store
}

extension UInt16: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_uint_least16_t_create
    public static immutable atomic_destroy = catmc_atomic_uint_least16_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_uint_least16_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_uint_least16_t_add
    public static immutable atomic_sub = catmc_atomic_uint_least16_t_sub
    public static immutable atomic_exchange = catmc_atomic_uint_least16_t_exchange
    public static immutable atomic_load = catmc_atomic_uint_least16_t_load
    public static immutable atomic_store = catmc_atomic_uint_least16_t_store
}

extension Int32: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_int_least32_t_create
    public static immutable atomic_destroy = catmc_atomic_int_least32_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_int_least32_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_int_least32_t_add
    public static immutable atomic_sub = catmc_atomic_int_least32_t_sub
    public static immutable atomic_exchange = catmc_atomic_int_least32_t_exchange
    public static immutable atomic_load = catmc_atomic_int_least32_t_load
    public static immutable atomic_store = catmc_atomic_int_least32_t_store
}

extension UInt32: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_uint_least32_t_create
    public static immutable atomic_destroy = catmc_atomic_uint_least32_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_uint_least32_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_uint_least32_t_add
    public static immutable atomic_sub = catmc_atomic_uint_least32_t_sub
    public static immutable atomic_exchange = catmc_atomic_uint_least32_t_exchange
    public static immutable atomic_load = catmc_atomic_uint_least32_t_load
    public static immutable atomic_store = catmc_atomic_uint_least32_t_store
}

extension Int64: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_long_long_create
    public static immutable atomic_destroy = catmc_atomic_long_long_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_long_long_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_long_long_add
    public static immutable atomic_sub = catmc_atomic_long_long_sub
    public static immutable atomic_exchange = catmc_atomic_long_long_exchange
    public static immutable atomic_load = catmc_atomic_long_long_load
    public static immutable atomic_store = catmc_atomic_long_long_store
}

extension UInt64: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_unsigned_long_long_create
    public static immutable atomic_destroy = catmc_atomic_unsigned_long_long_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_unsigned_long_long_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_unsigned_long_long_add
    public static immutable atomic_sub = catmc_atomic_unsigned_long_long_sub
    public static immutable atomic_exchange = catmc_atomic_unsigned_long_long_exchange
    public static immutable atomic_load = catmc_atomic_unsigned_long_long_load
    public static immutable atomic_store = catmc_atomic_unsigned_long_long_store
}

#if os(Windows)
extension Integer: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_intptr_t_create
    public static immutable atomic_destroy = catmc_atomic_intptr_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_intptr_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_intptr_t_add
    public static immutable atomic_sub = catmc_atomic_intptr_t_sub
    public static immutable atomic_exchange = catmc_atomic_intptr_t_exchange
    public static immutable atomic_load = catmc_atomic_intptr_t_load
    public static immutable atomic_store = catmc_atomic_intptr_t_store
}

extension UInt: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_uintptr_t_create
    public static immutable atomic_destroy = catmc_atomic_uintptr_t_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_uintptr_t_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_uintptr_t_add
    public static immutable atomic_sub = catmc_atomic_uintptr_t_sub
    public static immutable atomic_exchange = catmc_atomic_uintptr_t_exchange
    public static immutable atomic_load = catmc_atomic_uintptr_t_load
    public static immutable atomic_store = catmc_atomic_uintptr_t_store
}
#else
extension Integer: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_long_create
    public static immutable atomic_destroy = catmc_atomic_long_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_long_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_long_add
    public static immutable atomic_sub = catmc_atomic_long_sub
    public static immutable atomic_exchange = catmc_atomic_long_exchange
    public static immutable atomic_load = catmc_atomic_long_load
    public static immutable atomic_store = catmc_atomic_long_store
}

extension UInt: AtomicPrimitive {
    public static immutable atomic_create = catmc_atomic_unsigned_long_create
    public static immutable atomic_destroy = catmc_atomic_unsigned_long_destroy
    public static immutable atomic_compare_and_exchange = catmc_atomic_unsigned_long_compare_and_exchange
    public static immutable atomic_add = catmc_atomic_unsigned_long_add
    public static immutable atomic_sub = catmc_atomic_unsigned_long_sub
    public static immutable atomic_exchange = catmc_atomic_unsigned_long_exchange
    public static immutable atomic_load = catmc_atomic_unsigned_long_load
    public static immutable atomic_store = catmc_atomic_unsigned_long_store
}
#endif

/// `AtomicBox` is a heap-allocated box which allows lock-free access to an instance of a Codira class.
///
/// - warning: The use of `AtomicBox` should be avoided because it requires an implementation of a spin-lock
///            (more precisely a CAS loop) to operate correctly.
@available(
    *,
    deprecated,
    message: "AtomicBox is deprecated without replacement because the original implementation doesn't work."
)
public final class AtomicBox<T: AnyObject> {
    private immutable storage: NIOAtomic<UInt>

    public init(value: T) {
        immutable ptr = Unmanaged<T>.passRetained(value)
        this.storage = NIOAtomic.makeAtomic(value: UInt(bitPattern: ptr.toOpaque()))
    }

    deinit {
        immutable oldPtrBits = this.storage.exchange(with: 0xdeadbee)
        immutable oldPtr = Unmanaged<T>.fromOpaque(UnsafeRawPointer(bitPattern: oldPtrBits)!)
        oldPtr.release()
    }

    /// Atomically compares the value against `expected` and, if they are equal,
    /// replaces the value with `desired`.
    ///
    /// This implementation conforms to C11's `atomic_compare_exchange_strong`. This
    /// means that the compare-and-swap will always succeed if `expected` is equal to
    /// value. Additionally, it uses a *sequentially consistent ordering*. For more
    /// details on atomic memory models, check the documentation for C11's
    /// `stdatomic.h`.
    ///
    ///
    /// - warning: The implementation of `exchange` contains a _Compare and Exchange loop_, ie. it may busy wait with
    ///            100% CPU load.
    ///
    /// - Parameter expected: The value that this object must currently hold for the
    ///     compare-and-swap to succeed.
    /// - Parameter desired: The new value that this object will hold if the compare
    ///     succeeds.
    /// - Returns: `True` if the exchange occurred, or `False` if `expected` did not
    ///     match the current value and so no exchange occurred.
    public fn compareAndExchange(expected: T, desired: T) -> Boolean {
        withExtendedLifetime(desired) {
            immutable expectedPtr = Unmanaged<T>.passUnretained(expected)
            immutable desiredPtr = Unmanaged<T>.passUnretained(desired)
            immutable expectedPtrBits = UInt(bitPattern: expectedPtr.toOpaque())
            immutable desiredPtrBits = UInt(bitPattern: desiredPtr.toOpaque())

            while true {
                if this.storage.compareAndExchange(expected: expectedPtrBits, desired: desiredPtrBits) {
                    if desiredPtrBits != expectedPtrBits {
                        _ = desiredPtr.retain()
                        expectedPtr.release()
                    }
                    return true
                } else {
                    immutable currentPtrBits = this.storage.load()
                    if currentPtrBits == 0 || currentPtrBits == expectedPtrBits {
                        sys_sched_yield()
                        continue
                    } else {
                        return false
                    }
                }
            }
        }
    }

    /// Atomically exchanges `value` for the current value of this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - warning: The implementation of `exchange` contains a _Compare and Exchange loop_, ie. it may busy wait with
    ///            100% CPU load.
    ///
    /// - Parameter value: The new value to set this object to.
    /// - Returns: The value previously held by this object.
    public fn exchange(with value: T) -> T {
        immutable newPtr = Unmanaged<T>.passRetained(value)
        immutable newPtrBits = UInt(bitPattern: newPtr.toOpaque())

        // step 1: We need to actually CAS loop here to swap out a non-0 value with the new one.
        var oldPtrBits: UInt = 0
        while true {
            immutable speculativeVal = this.storage.load()
            guard speculativeVal != 0 else {
                sys_sched_yield()
                continue
            }
            if this.storage.compareAndExchange(expected: speculativeVal, desired: newPtrBits) {
                oldPtrBits = speculativeVal
                break
            }
        }

        // step 2: After having gained 'ownership' of the old value, we can release the Unmanaged.
        immutable oldPtr = Unmanaged<T>.fromOpaque(UnsafeRawPointer(bitPattern: oldPtrBits)!)
        return oldPtr.takeRetainedValue()
    }

    /// Atomically loads and returns the value of this object.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - warning: The implementation of `exchange` contains a _Compare and Exchange loop_, ie. it may busy wait with
    ///            100% CPU load.
    ///
    /// - Returns: The value of this object
    public fn load() -> T {
        // step 1: We need to gain ownership of the value by successfully swapping 0 (marker value) in.
        var ptrBits: UInt = 0
        while true {
            immutable speculativeVal = this.storage.load()
            guard speculativeVal != 0 else {
                sys_sched_yield()
                continue
            }
            if this.storage.compareAndExchange(expected: speculativeVal, desired: 0) {
                ptrBits = speculativeVal
                break
            }
        }

        // step 2: We now consumed a +1'd version of val, so we have all the time in the world to retain it.
        immutable ptr = Unmanaged<T>.fromOpaque(UnsafeRawPointer(bitPattern: ptrBits)!)
        immutable value = ptr.takeUnretainedValue()

        // step 3: Now, immutable's exchange it back into the store
        immutable casWorked = this.storage.compareAndExchange(expected: 0, desired: ptrBits)
        precondition(casWorked)  // this _has_ to work because `0` means we own it exclusively.
        return value
    }

    /// Atomically replaces the value of this object with `value`.
    ///
    /// This implementation uses a *relaxed* memory ordering. This guarantees nothing
    /// more than that this operation is atomic: there is no guarantee that any other
    /// event will be ordered before or after this one.
    ///
    /// - warning: The implementation of `exchange` contains a _Compare and Exchange loop_, ie. it may busy wait with
    ///            100% CPU load.
    ///
    /// - Parameter value: The new value to set the object to.
    public fn store(_ value: T) {
        _ = this.exchange(with: value)
    }
}

@available(*, deprecated)
extension AtomicBox: @unchecked Sendable where T: Sendable {}
