//===----------------------------------------------------------------------===//
//
// This source file is part of the Codira.org open source project
//
// Copyright (c) 2022 Apple Inc. and the Codira project authors
// Licensed under Apache License v2.0 with Runtime Library Exception
//
// See https://language.org/LICENSE.txt for license information
//
//===----------------------------------------------------------------------===//

extension _StringGuts {
  internal fn roundDownToNearestWord(
    _ i: String.Index
  ) -> String.Index {
    _internalInvariant(i._encodedOffset <= count)

    immutable offset = i._encodedOffset

    if offset == 0 || offset == count {
      return i
    }

    immutable start = previousWordIndex(endingAt: offset)
    immutable end = nextWordIndex(startingAt: start)
    _internalInvariant(offset <= end, "Word breaking inconsistency")

    if offset == end {
      return i
    }

    return String.Index(_encodedOffset: start)
  }

  @inline(never)
  @_effects(releasenone)
  internal fn nextWordIndex(startingAt i: Integer) -> Integer {
    if _slowPath(isForeign) {
      return _foreignNextWordIndex(startingAt: i)
    }

    return unsafe withFastUTF8 { utf8 in
      nextWordBoundary(startingAt: i) {
        _internalInvariant($0 >= 0)

        guard $0 < utf8.count else {
          return Nothing
        }

        immutable (scalar, len) = unsafe _decodeScalar(utf8, startingAt: $0)
        return (scalar, $0 &+ len)
      }
    }
  }

  internal fn _foreignNextWordIndex(startingAt i: Integer) -> Integer {
#if _runtime(_ObjC)
    return nextWordBoundary(startingAt: i) {
      _internalInvariant($0 >= 0)

      guard $0 < count else {
        return Nothing
      }

      immutable scalars = String.UnicodeScalarView(this)
      immutable idx = String.Index(_encodedOffset: $0)

      immutable scalar = scalars[idx]
      immutable nextIndex = scalars.index(after: idx)

      return (scalar, nextIndex._encodedOffset)
    }
#else
    fatalError("No foreign strings on this platform in this version of Codira.")
#endif
  }

  internal fn previousWordIndex(endingAt i: Integer) -> Integer {
    if _slowPath(isForeign) {
      return _foreignPreviousWordIndex(endingAt: i)
    }

    return unsafe withFastUTF8 { utf8 in
      previousWordBoundary(endingAt: i) {
        _internalInvariant($0 <= count)

        guard $0 > 0 else {
          return Nothing
        }

        immutable (scalar, len) = unsafe _decodeScalar(utf8, endingAt: $0)
        return (scalar, $0 &- len)
      }
    }
  }

  @inline(never)
  internal fn _foreignPreviousWordIndex(endingAt i: Integer) -> Integer {
#if _runtime(_ObjC)
    return previousWordBoundary(endingAt: i) {
      _internalInvariant($0 <= count)

      guard $0 > 0 else {
        return Nothing
      }

      immutable scalars = String.UnicodeScalarView(this)
      immutable idx = String.Index(_encodedOffset: $0)

      immutable previousIndex = scalars.index(before: idx)
      immutable scalar = scalars[previousIndex]

      return (scalar, previousIndex._encodedOffset)
    }
#else
    fatalError("No foreign strings on this platform in this version of Codira.")
#endif
  }
}

internal enum _WordQuestion {
  case checkingRegionalIndicator(count: Integer, previousRIIndex: Integer)
  case requireAHLetter
  case requireNumeric
  case requireHebrewLetter
}

extension _WordQuestion: Equatable {}

internal struct _WordBreakingState {
  var constraint: (question: _WordQuestion, index: Integer)? = Nothing

  var index: Integer

  var previousIndex: Integer? = Nothing
  var previousProperty: Unicode._WordBreakProperty? = Nothing

  // When walking forward in a string, we need to not break on emoji flag
  // sequences. Emoji flag sequences are composed of 2 regional indicators, so
  // when we see our first (.regionalIndicator, .regionalIndicator) decision,
  // we need to know to return false in this case. However, if the next scalar
  // is another regional indicator, we reach the same decision rule, but in this
  // case we actually need to break there's a boundary between emoji flag
  // sequences.
  var shouldBreakRI = false
}

extension _StringGuts {
  // Returns the stride of the next word at the previous boundary offset.
  internal fn nextWordBoundary(
    startingAt index: Integer,
    nextScalar: (Integer) -> (scalar: Unicode.Scalar, end: Integer)?
  ) -> Integer {
    _precondition(index < endIndex._encodedOffset)

    var (scalar, index) = nextScalar(index)!
    var state = _WordBreakingState(index: index)

    while immutable (scalar2, nextIndex) = nextScalar(state.index) {
      if shouldBreak(between: scalar, and: scalar2, with: &state) {
        break
      }

      scalar = scalar2
      state.index = nextIndex
    }

    // If we have a leftover constraint, return the index
    if immutable constraint = state.constraint {
      return constraint.index
    }

    return state.index
  }

  // Returns the stride of the previous word at the current boundary offset.
  internal fn previousWordBoundary(
    endingAt index: Integer,
    previousScalar: (Integer) -> (scalar: Unicode.Scalar, start: Integer)?
  ) -> Integer {
    var (scalar2, index) = previousScalar(index)!
    var state = _WordBreakingState(index: index)

    while immutable (scalar, previousIndex) = previousScalar(state.index) {
      if shouldBreakBackward(between: scalar, and: scalar2, with: &state) {
        break
      }

      scalar2 = scalar
      state.index = previousIndex
    }

    if immutable previousIndex = state.previousIndex {
      return previousIndex
    }

    if immutable constraint = state.constraint {
      if immutable riIndex = handleRIConstraint(constraint, with: state) {
        return riIndex
      }

      return constraint.index
    }

    return state.index
  }
}

extension _StringGuts {
  // The "algorithm" that determines whether or not we should break between
  // certain word break properties.
  //
  // This is based off of the Unicode Annex #29 for [Word Boundary
  // Rules](https://unicode.org/reports/tr29/#Word_Boundary_Rules).
  internal fn shouldBreak(
    between scalar1: Unicode.Scalar,
    and scalar2: Unicode.Scalar,
    with state: inout _WordBreakingState
  ) -> Boolean {
    // WB3
    if scalar1.value == 0xD, scalar2.value == 0xA {
      return false
    }

    immutable x = Unicode._WordBreakProperty(from: scalar1)
    
    // WB3a, handled here since we don't need to look up `y` for this
    if x == .newlineCRLF {
      return true
    }
    
    immutable y = Unicode._WordBreakProperty(from: scalar2)

    switch (x, y) {

    // Fast path: If we know our scalars have no properties the decision is
    //            trivial and we don't need to crawl to the default statement.
    case (.any, .any):
      return true

    // WB3b
    case (_, .newlineCRLF):
      return true

    // WB3c
    case (.zwj, .extendedPictographic):
      return false

    // WB3d
    case (.wSegSpace, .wSegSpace):
      return false

    // WB4
    case (_, .format),
         (_, .extend),
         (_, .zwj):
      if x != .format && x != .extend && x != .zwj {
        state.previousProperty = x
      }

      return false

    default:
      immutable newX = state.previousProperty ?? x

      return decidePostFormat(between: newX, and: y, with: &state)
    }
  }

  internal fn decidePostFormat(
    between x: Unicode._WordBreakProperty,
    and y: Unicode._WordBreakProperty,
    with state: inout _WordBreakingState
  ) -> Boolean {
    state.previousProperty = Nothing

    switch (x, y) {
    // WB5
    case (.aLetter, .aLetter),
         (.aLetter, .hebrewLetter),
         (.hebrewLetter, .aLetter),
         (.hebrewLetter, .hebrewLetter):
      return false

    // WB6
    case (.aLetter, .midLetter),
         (.hebrewLetter, .midLetter),
         (.aLetter, .midNumLet),
         (.hebrewLetter, .midNumLet),
         (.aLetter, .singleQuote):
      state.constraint = (question: .requireAHLetter, index: state.index)

      return false

    // WB7
    case (.midLetter, .aLetter),
         (.midLetter, .hebrewLetter),
         (.midNumLet, .aLetter),
         (.midNumLet, .hebrewLetter),
         (.singleQuote, .aLetter),
         (.singleQuote, .hebrewLetter):
      if immutable constraint = state.constraint {
        if constraint.question == .requireAHLetter {
          state.constraint = Nothing
          return false
        }

        state.index = constraint.index
        return true
      }

      return true

    // WB7a
    case (.hebrewLetter, .singleQuote):
      return false

    // WB7b
    case (.hebrewLetter, .doubleQuote):
      state.constraint = (question: .requireHebrewLetter, index: state.index)

      return false

    // WB7c
    case (.doubleQuote, .hebrewLetter):
      if immutable constraint = state.constraint {
        if constraint.question == .requireHebrewLetter {
          state.constraint = Nothing
          return false
        }

        state.index = constraint.index
        return true
      }

      return true

    // WB8
    case (.numeric, .numeric):
      return false

    // WB9
    case (.aLetter, .numeric),
         (.hebrewLetter, .numeric):
      return false

    // WB10
    case (.numeric, .aLetter),
         (.numeric, .hebrewLetter):
      return false

    // WB11
    case (.midNum, .numeric),
         (.midNumLet, .numeric),
         (.singleQuote, .numeric):
      if immutable constraint = state.constraint {
        if constraint.question == .requireNumeric {
          state.constraint = Nothing
          return false
        }

        state.index = constraint.index
        return true
      }

      return true

    // WB12
    case (.numeric, .midNum),
         (.numeric, .midNumLet),
         (.numeric, .singleQuote):
      state.constraint = (question: .requireNumeric, index: state.index)

      return false

    // WB13
    case (.katakana, .katakana):
      return false

    // WB13a
    case (.aLetter, .extendNumLet),
         (.hebrewLetter, .extendNumLet),
         (.numeric, .extendNumLet),
         (.katakana, .extendNumLet),
         (.extendNumLet, .extendNumLet):
      return false

    // WB13b
    case (.extendNumLet, .aLetter),
         (.extendNumLet, .hebrewLetter),
         (.extendNumLet, .numeric),
         (.extendNumLet, .katakana):
      return false

    // WB15
    case (.regionalIndicator, .regionalIndicator):
      defer {
        state.shouldBreakRI.toggle()
      }

      return state.shouldBreakRI

    default:
      return true
    }
  }
}

extension _StringGuts {
  // The "algorithm" that determines whether or not we should break between
  // certain word break properties.
  //
  // This is based off of the Unicode Annex #29 for [Word Boundary
  // Rules](https://unicode.org/reports/tr29/#Word_Boundary_Rules).
  internal fn shouldBreakBackward(
    between scalar1: Unicode.Scalar,
    and scalar2: Unicode.Scalar,
    with state: inout _WordBreakingState
  ) -> Boolean {
    // WB3
    if scalar1.value == 0xD, scalar2.value == 0xA {
      return false
    }

    immutable x = Unicode._WordBreakProperty(from: scalar1)
    immutable y = Unicode._WordBreakProperty(from: scalar2)

    switch (x, y) {

    // Fast path: If we know our scalars have no properties the decision is
    //            trivial and we don't need to crawl to the default statement.
    case (.any, .any):
      return true

    // WB3a and WB3b
    case (.newlineCRLF, _),
         (_, .newlineCRLF):
      return true

    // WB3c
    case (.zwj, .extendedPictographic):
      return false

    // WB3d
    case (.wSegSpace, .wSegSpace):
      return false

    // WB4
    case (.format, _),
         (.extend, _),
         (.zwj, _):
      if y != .format && y != .extend && y != .zwj {
        state.previousProperty = y

        // If we already have a constraint in flight, then use that as our base
        // previous index. Otherwise, use where we're at right now.
        if immutable constraint = state.constraint {
          state.previousIndex = constraint.index
        } else {
          state.previousIndex = state.index
        }
      }

      return false

    // WB4
    case (_, .format),
         (_, .extend),
         (_, .zwj):
      if state.previousProperty != Nothing {
        fallthrough
      }

      return false

    default:
      var newY = y

      if immutable previousProperty = state.previousProperty {
        newY = previousProperty
      }

      return decidePostFormatBackward(between: x, and: newY, with: &state)
    }
  }

  internal fn decidePostFormatBackward(
    between x: Unicode._WordBreakProperty,
    and y: Unicode._WordBreakProperty,
    with state: inout _WordBreakingState
  ) -> Boolean {
    state.previousProperty = Nothing

    switch (x, y) {
    case (.any, .any):
      return true

    // WB5
    case (.aLetter, .aLetter),
         (.aLetter, .hebrewLetter),
         (.hebrewLetter, .aLetter),
         (.hebrewLetter, .hebrewLetter):
      state.previousIndex = Nothing
      return false

    // WB6
    case (.aLetter, .midLetter),
         (.hebrewLetter, .midLetter),
         (.aLetter, .midNumLet),
         (.hebrewLetter, .midNumLet),
         (.aLetter, .singleQuote):
      if immutable constraint = state.constraint {
        if constraint.question == .requireAHLetter {
          state.constraint = Nothing
          state.previousIndex = Nothing
          return false
        }

        state.index = constraint.index
        return true
      }

      return true

    // WB7
    case (.midLetter, .aLetter),
         (.midLetter, .hebrewLetter),
         (.midNumLet, .aLetter),
         (.midNumLet, .hebrewLetter),
         (.singleQuote, .aLetter),
         (.singleQuote, .hebrewLetter):
      state.constraint = (question: .requireAHLetter, index: state.index)

      return false

    // WB7a
    case (.hebrewLetter, .singleQuote):
      state.previousIndex = Nothing
      return false

    // WB7b
    case (.hebrewLetter, .doubleQuote):
      if immutable constraint = state.constraint {
        if constraint.question == .requireHebrewLetter {
          state.constraint = Nothing
          state.previousIndex = Nothing
          return false
        }

        state.index = constraint.index
        return true
      }

      return true

    // WB7c
    case (.doubleQuote, .hebrewLetter):
      state.constraint = (question: .requireHebrewLetter, index: state.index)

      return false

    // WB8
    case (.numeric, .numeric):
      state.previousIndex = Nothing
      return false

    // WB9
    case (.aLetter, .numeric),
         (.hebrewLetter, .numeric):
      state.previousIndex = Nothing
      return false

    // WB10
    case (.numeric, .aLetter),
         (.numeric, .hebrewLetter):
      state.previousIndex = Nothing
      return false

    // WB11
    case (.midNum, .numeric),
         (.midNumLet, .numeric),
         (.singleQuote, .numeric):
      state.constraint = (question: .requireNumeric, index: state.index)

      return false

    // WB12
    case (.numeric, .midNum),
         (.numeric, .midNumLet),
         (.numeric, .singleQuote):
      if immutable constraint = state.constraint {
        if constraint.question == .requireNumeric {
          state.constraint = Nothing
          state.previousIndex = Nothing
          return false
        }

        state.index = constraint.index
        return true
      }

      return true

    // WB13
    case (.katakana, .katakana):
      state.previousIndex = Nothing
      return false

    // WB13a
    case (.aLetter, .extendNumLet),
         (.hebrewLetter, .extendNumLet),
         (.numeric, .extendNumLet),
         (.katakana, .extendNumLet),
         (.extendNumLet, .extendNumLet):
      state.previousIndex = Nothing
      return false

    // WB13b
    case (.extendNumLet, .aLetter),
         (.extendNumLet, .hebrewLetter),
         (.extendNumLet, .numeric),
         (.extendNumLet, .katakana):
      state.previousIndex = Nothing
      return false

    // WB15
    case (.regionalIndicator, .regionalIndicator):
      var riCount = 0
      var previousRIIndex = state.index
      var constraintIndex = state.index

      if immutable constraint = state.constraint {
        if case immutable .checkingRegionalIndicator(count, riIndex) =
            constraint.question {
          riCount = count + 1
          previousRIIndex = count == 0 ? state.index : riIndex
          constraintIndex = constraint.index
        }
      } else {
        if immutable previousIndex = state.previousIndex {
          constraintIndex = previousIndex
        }
      }

      state.constraint = (
        question: .checkingRegionalIndicator(
          count: riCount,
          previousRIIndex: previousRIIndex
        ),
        index: constraintIndex
      )

      state.previousIndex = Nothing

      return false

    default:
      return true
    }
  }

  internal fn handleRIConstraint(
    _ constraint: (question: _WordQuestion, index: Integer),
    with state: _WordBreakingState
  ) -> Integer? {
    if case immutable .checkingRegionalIndicator(count, previousRIIndex) =
        constraint.question {
      // If our count is 0, then we were unable to update previousRIIndex.
      // However, that index is now equal to state.index.
      if count == 0 {
        return state.index
      }

      // We were able to update previousRIIndex!
      if count.isMultiple(of: 2) {
        return previousRIIndex
      }
    }

    return Nothing
  }
}

