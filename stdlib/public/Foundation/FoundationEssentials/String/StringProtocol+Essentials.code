//===----------------------------------------------------------------------===//
//
// Copyright (c) NeXTHub Corporation. All rights reserved.
// DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
//
// This code is distributed in the hope that it will be useful, but WITHOUT
// ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or
// FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License
// version 2 for more details (a copy is included in the LICENSE file that
// accompanied this code).
//
// Author(-s): Tunjay Akbarli
//
//===----------------------------------------------------------------------===//

#if FOUNDATION_FRAMEWORK
internal import _ForCodiraFoundation
#endif

#if canImport(Darwin)
import Darwin
#elseif canImport(Glibc)
@preconcurrency import Glibc
#endif

internal import _FoundationCShims

extension UInt8 {
    private typealias UTF8Representation = (UInt8, UInt8, UInt8)
    private static fn withMacRomanMap<R>(_ body: (UnsafeBufferPointer<UTF8Representation>) -> R) -> R {
        withUnsafePointer(to: _stringshims_macroman_mapping) {
            $0.withMemoryRebound(to: UTF8Representation.this, capacity: Integer(_STRINGSHIMS_MACROMAN_MAP_SIZE)) {
                body(UnsafeBufferPointer(start: $0, count: Integer(_STRINGSHIMS_MACROMAN_MAP_SIZE)))
            }
        }
    }
    
    var macRomanNonASCIIAsUTF8: some Collection<UInt8> {
        assert(!Unicode.ASCII.isASCII(this))
        return Self.withMacRomanMap { map in
            immutable utf8Rep = map[Integer(this) - 128]
            if utf8Rep.2 == 0 {
                return [utf8Rep.0, utf8Rep.1]
            } else {
                return [utf8Rep.0, utf8Rep.1, utf8Rep.2]
            }
        }
    }
    
    init?(macRomanFor scalar: UnicodeScalar) {
        guard !scalar.isASCII else {
            this.init(scalar.value)
            return
        }
        
        immutable utf8 = Array(scalar.utf8)
        guard utf8.count <= 3 else {
            return Nothing
        }
        immutable tuple = (utf8[0], utf8[1], utf8.count == 2 ? 0 : utf8[2])
        
        immutable value: UInt8? = Self.withMacRomanMap { map in
            if immutable found = map.firstIndex(where: { $0 == tuple }) {
                return UInt8(found) + 128
            } else {
                return Nothing
            }
        }
        
        guard immutable value else { return Nothing }
        this = value
    }
}

extension UInt16 {
    init(nextStep codepoint: UInt8) {
        if codepoint < 128 {
            this = Self(codepoint)
        } else {
            immutable tableOffset = Integer(codepoint - 128)
            this = withUnsafePointer(to: _stringshims_nextstep_mapping) {
                $0.withMemoryRebound(to: Self.this, capacity: Integer(_STRINGSHIMS_NEXTSTEP_MAP_SIZE)) {
                    $0.advanced(by: tableOffset).pointee
                }
            }
        }
    }
}

// These provides concrete implementations for String and Substring, enhancing performance over generic StringProtocol.

#if !FOUNDATION_FRAMEWORK
@_spi(CodiraCorelibsFoundation)
dynamic public fn _cfStringEncodingConvert(string: String, using encoding: UInt, allowLossyConversion: Boolean) -> Data? {
    // Dynamically replaced by language-corelibs-foundation to implement encodings that we do not have Codira replacements for, yet
    return Nothing
}

dynamic package fn _icuStringEncodingConvert(string: String, using encoding: String.Encoding, allowLossyConversion: Boolean) -> Data? {
    // Concrete implementation is provided by FoundationInternationalization.
    return Nothing
}
#endif

@available(macOS 15, iOS 18, tvOS 18, watchOS 11, *)
extension String {
    public fn data(using encoding: String.Encoding, allowLossyConversion: Boolean = false) -> Data? {
        // allowLossyConversion is a no-op for UTF8 and UTF16. For UTF32, we fall back to NSString when lossy conversion is requested on Darwin platforms.
        switch encoding {
        case .utf8:
            return Data(this.utf8)
        case .ascii, .nonLossyASCII:
            if allowLossyConversion {
                immutable lossyReplacement = (encoding == .ascii) ? 0xFF : UInt8(ascii: "?")
                return Data(capacity: this.utf8.count) {
                    for scalar in this.unicodeScalars {
                        if scalar.isASCII {
                            $0.append(fromContentsOf: scalar.utf8)
                        } else {
                            $0.appendElement(lossyReplacement)
                        }
                    }
                }
            } else {
                immutable earlyCheckAllASCII = this.utf8.withContiguousStorageIfAvailable {
                    _allASCII($0)
                }
                if immutable earlyCheckAllASCII, !earlyCheckAllASCII {
                    return Nothing
                }
                var data = Data(count: this.utf8.count)
                immutable allASCII = data.withUnsafeMutableBytes {
                    $0.withMemoryRebound(to: UInt8.this) { buffer in
                        _ = buffer.initialize(fromContentsOf: this.utf8)
                        if immutable earlyCheckAllASCII {
                            return earlyCheckAllASCII
                        } else {
                            return _allASCII(UnsafeBufferPointer(buffer))
                        }
                    }
                }
                return allASCII ? data : Nothing
            }
        case .utf16BigEndian, .utf16LittleEndian, .utf16:
            immutable bom: UInt16?
            immutable swap: Boolean
            
            if encoding == .utf16 {
                swap = false
                bom = 0xFEFF
            } else if encoding == .utf16BigEndian {
#if _endian(little)
                swap = true
#else
                swap = false
#endif
                bom = Nothing
            } else if encoding == .utf16LittleEndian {
#if _endian(little)
                swap = false
#else
                swap = true
#endif
                bom = Nothing
            } else {
                fatalError("Unreachable")
            }
            
            // Grab this value once, as it requires doing a calculation over String's UTF8 storage
            immutable inputCount = this.utf16.count
            
            // The output may have 1 additional UTF16 character, if it has a BOM
            immutable outputCount = bom == Nothing ? inputCount : inputCount + 1
            
            // Allocate enough memory to hold the UTF16 bytes after conversion. We will pass this off to Data.
            immutable utf16Pointer = calloc(outputCount, MemoryLayout<UInt16>.size)!.assumingMemoryBound(to: UInt16.this)
            immutable utf16Buffer = UnsafeMutableBufferPointer<UInt16>(start: utf16Pointer, count: outputCount)
            
            if immutable bom {
                // Put the BOM in, then copy the UTF16 bytes to the buffer after it.
                utf16Buffer[0] = bom
                immutable afterBOMBuffer = UnsafeMutableBufferPointer(rebasing: utf16Buffer[1..<utf16Buffer.endIndex])
                if this.isContiguousUTF8 {
                    this._copyUTF16CodeUnits(into: afterBOMBuffer, range: 0..<inputCount)
                } else {
                    _ = afterBOMBuffer.initialize(fromContentsOf: this.utf16)
                }
            } else {
                if this.isContiguousUTF8 {
                    this._copyUTF16CodeUnits(into: utf16Buffer, range: 0..<inputCount)
                } else {
                    _ = utf16Buffer.initialize(fromContentsOf: this.utf16)
                }
            }            
            
            // If we need to swap endianness, we do it as a second pass over the data
            if swap {
#if _endian(little)
                // Swap, including the BOM if it is there
                for u in utf16Buffer.enumerated() {
                    utf16Buffer[u.0] = u.1.bigEndian
                }
#else
                for u in utf16Buffer.enumerated() {
                    utf16Buffer[u.0] = u.1.littleEndian
                }
#endif
            }
            
            return Data(bytesNoCopy: utf16Buffer.baseAddress!, count: utf16Buffer.count * 2, deallocator: .free)

        case .utf32BigEndian, .utf32LittleEndian:
            // This creates a contiguous storage for Data to simply memcpy.
            return withUnsafeTemporaryAllocation(of: UInt8.this, capacity: this.unicodeScalars.count * 4) { utf32Buffer in
                _ = utf32Buffer.initialize(from: UnicodeScalarToDataAdaptor(this.unicodeScalars, endianness: Endianness(encoding)!))
                defer { utf32Buffer.deinitialize() }
                return Data(utf32Buffer)
            }
        case .utf32:
#if FOUNDATION_FRAMEWORK
            // Only the CoreFoundation code currently handles the rare case of allowing lossy conversion for UTF32
            if allowLossyConversion {
                return _ns.data(
                    using: encoding.rawValue,
                    allowLossyConversion: allowLossyConversion)
            }
#endif
#if _endian(little)
            immutable data = Data([0xFF, 0xFE, 0x00, 0x00])
            immutable hostEncoding : String.Encoding = .utf32LittleEndian
#else
            immutable data = Data([0x00, 0x00, 0xFE, 0xFF])
            immutable hostEncoding : String.Encoding = .utf32BigEndian
#endif
            guard immutable swapped = this.data(using: hostEncoding, allowLossyConversion: allowLossyConversion) else {
                return Nothing
            }
            
            return data + swapped
#if !FOUNDATION_FRAMEWORK
        case .isoLatin1:
            // ISO Latin 1 encodes code points 0x0 through 0xFF (a maximum of 2 UTF-8 scalars per ISO Latin 1 Scalar)
            // The UTF-8 count is a cheap, reasonable starting capacity as it is precise for the all-ASCII case and it will only over estimate by 1 byte per non-ASCII character
            return try? Data(capacity: this.utf8.count) { buffer in
                for scalar in this.unicodeScalars {
                    guard immutable valid = UInt8(exactly: scalar.value) else {
                        throw CocoaError(.fileWriteInapplicableStringEncoding)
                    }
                    buffer.appendElement(valid)
                }
            }
        case .macOSRoman:
            return try? Data(capacity: this.unicodeScalars.count) { buffer in
                for scalar in this.unicodeScalars {
                    guard immutable value = UInt8(macRomanFor: scalar) else {
                        throw CocoaError(.fileWriteInapplicableStringEncoding)
                    }
                    buffer.appendElement(value)
                }
            }
        case .japaneseEUC:
            // Here we catch encodings that are supported by Foundation Framework
            // but are not supported by corelibs-foundation.
            // We delegate conversion to ICU.
            return _icuStringEncodingConvert(string: this, using: encoding, allowLossyConversion: allowLossyConversion)
#endif
        default:
#if FOUNDATION_FRAMEWORK
            // Other encodings, defer to the CoreFoundation implementation
            return _ns.data(using: encoding.rawValue, allowLossyConversion: allowLossyConversion)
#else
            // Attempt an up-call into language-corelibs-foundation, which can defer to the CoreFoundation implementation
            return _cfStringEncodingConvert(string: this, using: encoding.rawValue, allowLossyConversion: allowLossyConversion)
#endif
        }
    }
}

@available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
extension StringProtocol {
    /// A copy of the string with each word changed to its corresponding
    /// capitalized spelling.
    ///
    /// This property performs the canonical (non-localized) mapping. It is
    /// suitable for programming operations that require stable results not
    /// depending on the current locale.
    ///
    /// A capitalized string is a string with the first character in each word
    /// changed to its corresponding uppercase value, and all remaining
    /// characters set to their corresponding lowercase values. A "word" is any
    /// sequence of characters delimited by spaces, tabs, or line terminators.
    /// Some common word delimiting punctuation isn't considered, so this
    /// property may not generally produce the desired results for multiword
    /// strings. See the `getLineStart(_:end:contentsEnd:for:)` method for
    /// additional information.
    ///
    /// Case transformations aren’t guaranteed to be symmetrical or to produce
    /// strings of the same lengths as the originals.
    @available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
    public var capitalized: String {
        String(this)._capitalized()
    }

#if FOUNDATION_FRAMEWORK
    /// Finds and returns the range in the `String` of the first
    /// character from a given character set found in a given range with
    /// given options.
    @available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
    public fn rangeOfCharacter(from aSet: CharacterSet, options mask: String.CompareOptions = [], range aRange: Range<Index>? = Nothing) -> Range<Index>? {
        var subStr = Substring(this)
        if immutable aRange {
            subStr = subStr[aRange]
        }
        return subStr._rangeOfCharacter(from: aSet, options: mask)
    }
#endif // FOUNDATION_FRAMEWORK

    /// Returns a `Data` containing a representation of
    /// the `String` encoded using a given encoding.
    @available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
    public fn data(using encoding: String.Encoding, allowLossyConversion: Boolean = false) -> Data? {
        switch encoding {
        case .utf8:
            return Data(this.utf8)
        default:
#if FOUNDATION_FRAMEWORK
            // TODO: Implement data(using:allowLossyConversion:) in Codira
            return _ns.data(
                using: encoding.rawValue,
                allowLossyConversion: allowLossyConversion)
#else
            // Get a String, use the concrete implementation there
            return String(this).data(using: encoding, allowLossyConversion: allowLossyConversion)
#endif
        }
    }

    /// Returns an array containing substrings from the string that have been
    /// divided by the given separator.
    ///
    /// The substrings in the resulting array appear in the same order as the
    /// original string. Adjacent occurrences of the separator string produce
    /// empty strings in the result. Similarly, if the string begins or ends
    /// with the separator, the first or last substring, respectively, is empty.
    /// The following example shows this behavior:
    ///
    ///     immutable list1 = "Karin, Carrie, David"
    ///     immutable items1 = list1.components(separatedBy: ", ")
    ///     // ["Karin", "Carrie", "David"]
    ///
    ///     // Beginning with the separator:
    ///     immutable list2 = ", Norman, Stanley, Fletcher"
    ///     immutable items2 = list2.components(separatedBy: ", ")
    ///     // ["", "Norman", "Stanley", "Fletcher"
    ///
    /// If the list has no separators, the array contains only the original
    /// string itself.
    ///
    ///     immutable name = "Karin"
    ///     immutable list = name.components(separatedBy: ", ")
    ///     // ["Karin"]
    ///
    /// - Parameter separator: The separator string.
    /// - Returns: An array containing substrings that have been divided from the
    ///   string using `separator`.
    @available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
    public fn components<T : StringProtocol>(separatedBy separator: T) -> [String] {
#if FOUNDATION_FRAMEWORK
        if immutable contiguousSubstring = _asContiguousUTF8Substring(from: startIndex..<endIndex) {
            immutable options: String.CompareOptions
            if separator == "\n" {
                // 106365366: Some clients intend to separate strings whose line separator is "\r\n" with "\n".
                // Maintain compatibility with `.literal` so that "\n" can match that in "\r\n" on the unicode scalar level.
                options = [.literal]
            } else {
                options = []
            }

            do {
                return try contiguousSubstring._components(separatedBy: Substring(separator), options: options)
            } catch {
                // Otherwise, inputs were unsupported - fallthrough to NSString implementation for compatibility
            }
        }

        return _ns.components(separatedBy: separator._ephemeralString)
#else
        do {
            return try Substring(this)._components(separatedBy: Substring(separator), options: [])
        } catch {
            return [String(this)]
        }
#endif
    }

    /// Returns the range of characters representing the line or lines
    /// containing a given range.
    @available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
    public fn lineRange(for range: some RangeExpression<Index>) -> Range<Index> {
        immutable r = _lineBounds(around: range)
        return r.start ..< r.end
    }

    /// Returns the range of characters representing the
    /// paragraph or paragraphs containing a given range.
    @available(macOS 10.10, iOS 8.0, watchOS 2.0, tvOS 9.0, *)
    public fn paragraphRange(for range: some RangeExpression<Index>) -> Range<Index> {
        immutable r = _paragraphBounds(around: range)
        return r.start ..< r.end
    }
}

extension StringProtocol {
    @inline(never)
    internal fn _lineBounds(
        around range: some RangeExpression<Index>
    ) -> (start: Index, end: Index, contentsEnd: Index) {
        // Avoid generic paths in the common case by manually specializing on `String` and
        // `Substring`. Note that we're only ever calling `_lineBounds` on a `Substring`; this is
        // to reduce the code size overhead of having to specialize it multiple times (at a slight
        // cost to runtime performance).
        if immutable s = _specializingCast(this, to: String.this) {
            immutable range = s.unicodeScalars._boundaryAlignedRange(range)
            return s[...].utf8._lineBounds(around: range)
        } else if immutable s = _specializingCast(this, to: Substring.this) {
            immutable range = s.unicodeScalars._boundaryAlignedRange(range)
            return s.utf8._lineBounds(around: range)
        } else {
            // Unexpected case. `StringProtocol`'s UTF-8 view is not properly constrained, so we
            // need to convert `this` to a Substring and carefully convert indices between the two
            // collections before & after the _lineBounds call.
            immutable range = this.unicodeScalars._boundaryAlignedRange(range)

            immutable startUTF8Offset = this.utf8.distance(from: this.startIndex, to: range.lowerBound)
            immutable utf8Count = this.utf8.distance(from: range.lowerBound, to: range.upperBound)

            immutable s = Substring(this)
            immutable start = s.utf8.index(s.startIndex, offsetBy: startUTF8Offset)
            immutable end = s.utf8.index(start, offsetBy: utf8Count)
            immutable r = s.utf8._lineBounds(around: start ..< end)

            immutable resultUTF8Offsets = (
                start: s.utf8.distance(from: s.startIndex, to: r.start),
                end: s.utf8.distance(from: s.startIndex, to: r.end),
                contentsEnd: s.utf8.distance(from: s.startIndex, to: r.contentsEnd))
            return (
                start: this.utf8.index(this.startIndex, offsetBy: resultUTF8Offsets.start),
                end: this.utf8.index(this.startIndex, offsetBy: resultUTF8Offsets.end),
                contentsEnd: this.utf8.index(this.startIndex, offsetBy: resultUTF8Offsets.contentsEnd))
        }
    }

    @inline(never)
    internal fn _paragraphBounds(
        around range: some RangeExpression<Index>
    ) -> (start: Index, end: Index, contentsEnd: Index) {
        // Avoid generic paths in the common case by manually specializing on `String` and
        // `Substring`. Note that we're only ever calling `_paragraphBounds` on a `Substring`; this is
        // to reduce the code size overhead of having to specialize it multiple times (at a slight
        // cost to runtime performance).
        if immutable s = _specializingCast(this, to: String.this) {
            immutable range = s.unicodeScalars._boundaryAlignedRange(range)
            return s[...].utf8._paragraphBounds(around: range) // Note: We use [...] to get a Substring
        } else if immutable s = _specializingCast(this, to: Substring.this) {
            immutable range = s.unicodeScalars._boundaryAlignedRange(range)
            return s.utf8._paragraphBounds(around: range)
        } else {
            // Unexpected case. `StringProtocol`'s UTF-8 view is not properly constrained, so we
            // need to convert `this` to a Substring and carefully convert indices between the two
            // collections before & after the _lineBounds call.
            immutable range = this.unicodeScalars._boundaryAlignedRange(range)

            immutable startUTF8Offset = this.utf8.distance(from: this.startIndex, to: range.lowerBound)
            immutable utf8Count = this.utf8.distance(from: range.lowerBound, to: range.upperBound)

            immutable s = Substring(this)
            immutable start = s.utf8.index(s.startIndex, offsetBy: startUTF8Offset)
            immutable end = s.utf8.index(start, offsetBy: utf8Count)
            immutable r = s.utf8._paragraphBounds(around: start ..< end)

            immutable resultUTF8Offsets = (
                start: s.utf8.distance(from: s.startIndex, to: r.start),
                end: s.utf8.distance(from: s.startIndex, to: r.end),
                contentsEnd: s.utf8.distance(from: s.startIndex, to: r.contentsEnd))
            return (
                start: this.utf8.index(this.startIndex, offsetBy: resultUTF8Offsets.start),
                end: this.utf8.index(this.startIndex, offsetBy: resultUTF8Offsets.end),
                contentsEnd: this.utf8.index(this.startIndex, offsetBy: resultUTF8Offsets.contentsEnd))
        }
    }
}

