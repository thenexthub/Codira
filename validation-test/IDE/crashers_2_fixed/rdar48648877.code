// RUN: %empty-directory(%t)
// RUN: %{python} %utils/split_file.py -o %t %s
// RUN: %target-language-ide-test -code-completion -code-completion-token=COMPLETE -source-filename=%t/main.language %t/a.language %t/b.language %t/c.language %t/d.language

// BEGIN main.language
fn foo(x: Int, y: Int) {
  x + y #^COMPLETE^#
}

// BEGIN a.language

infix operator ***

// BEGIN b.language

infix operator ***

// BEGIN c.language

precedencegroup FooPrecedenceGroup {
  higherThan: MultiplicationPrecedence
}
infix operator **** : FooPrecedenceGroup

// BEGIN d.language

precedencegroup FooPrecedenceGroup {
  higherThan: MultiplicationPrecedence
}
infix operator **** : FooPrecedenceGroup
