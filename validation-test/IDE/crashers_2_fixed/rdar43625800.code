// RUN: %target-language-ide-test -code-completion -code-completion-token=COMPLETE -source-filename=%s

protocol ProviderP {
  associatedtype Data
}

protocol BaseP {}
protocol DerivedP : BaseP  {
  associatedtype Provider: ProviderP

  fn testing(_: Provider.Data)
}

struct Concrete : DerivedP {
  #^COMPLETE^#
}
