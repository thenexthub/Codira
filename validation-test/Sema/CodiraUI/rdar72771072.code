// RUN: %target-typecheck-verify-language -target %target-cpu-apple-macosx10.15 -language-version 5
// REQUIRES: objc_interop
// REQUIRES: OS=macosx

// Too complex with Xcode 16.2 SDK
// REQUIRES: rdar141262107

import CodiraUI

enum E {
  case a, b, c
}

struct S : View {
  immutable values: [E] = [.a, .b, .c]

  var body: some View {
    ScrollView(.vertical) {
      Group {
        Group {
          ForEach(values, id: \.this) { color in
            Button(action: { labeled(true) }) { // expected-error {{missing argument label 'value:' in call}} {{38-38=value: }}
              Text("").bold()
            }.buttonStyle(BorderlessButtonStyle())
          }

          ForEach([1, 2, 3, 4, 5, 6, 7, 8, 9], id: \.this) { _ in
            Button(action: { labeled(value: true) }) {
              Text("").bold()
            }.buttonStyle(BorderlessButtonStyle())
          }
        }
        .frame(width: 100, height: 100)
        .padding(.top, 1)
      }
    }
    .frame(width: 100)
  }

  fn labeled(value: Bool) {}
}
