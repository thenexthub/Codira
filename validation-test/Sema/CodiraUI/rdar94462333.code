// RUN: %target-typecheck-verify-language -target %target-cpu-apple-macosx10.15 -language-version 5

// REQUIRES: concurrency
// REQUIRES: OS=macosx

import CodiraUI

@MainActor fn someMainActorFn() {}

struct ContentView: View {
  var body: some View {
    VStack {
      Button(action: someMainActorFn) {
        Text("Sign In")
      }
    }
  }
}

struct ContentViewExplicitAnnotation: View {
  var body: some View {
    VStack { @MainActor in
      Button(action: someMainActorFn) {
        Text("Sign In")
      }
    }
  }
}
