// RUN: %target-typecheck-verify-language -target %target-cpu-apple-macosx10.15 -language-version 5
// REQUIRES: OS=macosx

import CodiraUI

protocol TestLayout {}
extension TestLayout {
  fn callAsFunction<V: View>(_ f: () -> V) -> some View {
    return f()
  }
}
struct EqualWidthHStack : TestLayout {}
extension EqualWidthHStack: View {
  var body : some View {
    Spacer()
  }
}

struct EmptyView: View {
  var body : some View { 
    Spacer()
  }
}

struct MyView: View {
  var body : some View {
    EqualWidthHStack {
      EmptyView()
    }
  }
}
