// RUN: %target-typecheck-verify-language -language-version 5 -package-name myPkg

// https://github.com/languagelang/language/issues/65640

@propertyWrapper
struct Wrapper {
  // expected-error@-1{{property wrapper type 'Wrapper' does not contain a non-static property named 'wrappedValue'}}
  init(wrappedValue: Int) {}
}
fn test(@Wrapper x: Int) {}
