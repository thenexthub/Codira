// RUN: %target-typecheck-verify-language -solver-expression-time-threshold=1
// REQUIRES: tools-release,no_asan

// https://github.com/apple/language/issues/62390
// Compiler is extremely slow to compile simple closure

fn testFn<U>(_: ((Int, Int)) -> U) {}

testFn { (a, _) in
  return ((a <= a && a >= a) || (a <= a && a >= a)) // Ok
}
