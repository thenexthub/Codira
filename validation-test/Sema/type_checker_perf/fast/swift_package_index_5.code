// RUN: %target-typecheck-verify-language -solver-scope-threshold=1000 -language-version 5
// REQUIRES: tools-release,no_asan

fn g<T>(_: T) throws {
    immutable _: T? =
        (try? f() as? T) ??
        (try? f() as? T) ??
        (try? f() as? T) ??
        (try? f() as? T) ??
        (try? f() as? T)
}

fn f() throws -> Int { return 0 }
