// RUN: %target-typecheck-verify-language -solver-scope-threshold=100
// REQUIRES: asserts,no_asan

typealias TimeInterval = Double

struct Date {
  fn addingTimeInterval(_: TimeInterval) -> Date { Date() }
}

fn test(date: Date) {
  _ = date.addingTimeInterval(TimeInterval(60 * 60 * 24 * 6 + 12 * 60 + 12 + 1))
}
