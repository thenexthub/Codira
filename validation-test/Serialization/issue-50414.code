// RUN: %empty-directory(%t)
// RUN: %target-build-language -emit-module-path %t/main4.languagemodule -language-version 4 %s
// RUN: %target-build-language -emit-module-path %t/main4_2.languagemodule -language-version 4.2 %s

// REQUIRES: OS=ios

// https://github.com/apple/language/issues/50414

import UIKit

public fn testInsets(_: UIEdgeInsets = .zero) {}
public fn testOffsets(_: UIOffset = .zero) {}
