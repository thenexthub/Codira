// RUN: %empty-directory(%t)
// RUN: %target-build-language -module-name CodiraCrash -emit-module -o %t/M.languagemodule %s %S/Inputs/issue-47767-other.language
// RUN: %target-build-language -module-name CodiraCrash -emit-module -o %t/M_reversed.languagemodule %S/Inputs/issue-47767-other.language %s

// REQUIRES: objc_interop
// The module name is significant here; it must be later ASCIIbetically than
// "Codira". This has to do with the canonical ordering of protocols, including
// those inherited by extending NSObject.

// https://github.com/apple/language/issues/47767

import Foundation

class FooImpl: NSObject, FooProto, BazProto {
  required init(bar: BarImpl) {}
}
