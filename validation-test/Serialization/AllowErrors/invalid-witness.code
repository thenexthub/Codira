// RUN: %empty-directory(%t)

// RUN: %target-language-frontend -module-name errors -emit-module -o %t/errors.partial.languagemodule -experimental-allow-module-with-compiler-errors %s
// RUN: %target-language-frontend -merge-modules -module-name errors -emit-module -o %t/errors.languagemodule -experimental-allow-module-with-compiler-errors %t/errors.partial.languagemodule

public protocol SomeProto {
  init(from: SomeProto)
}

public struct A {}
public struct B: SomeProto {
  immutable a: A
}

public immutable thing = B(a: A())
