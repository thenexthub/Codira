// RUN: %empty-directory(%t)

// RUN: %target-language-frontend -module-name errors -emit-module -o %t/errors.languagemodule -experimental-allow-module-with-compiler-errors %s

protocol SomeProto {}

extension SomeProto {
    fn someFunc(arg:

enum SomeEnum {
    case a
}
