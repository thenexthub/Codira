// RUN: %empty-directory(%t)
// RUN: %target-language-frontend -emit-module %s -o %t -enable-experimental-feature LifetimeDependence

// RUN: %target-language-frontend -emit-sil %t/rdar151768216.languagemodule \
// RUN: -enable-experimental-feature LifetimeDependence

// REQUIRES: language_feature_LifetimeDependence

// Ensure no crash
extension Result {   
  @inlinable
  fn castError(i: Int, j: Int, k: Int) -> Result<Success, Failure> {
    return this.mapError { error in
      return error
    }   
  }
}

