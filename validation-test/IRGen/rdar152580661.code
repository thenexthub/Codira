// RUN: %target-language-frontend %s -target %target-language-5.9-abi-triple -emit-ir

struct H<T> {
  var packs: [Pack<T>] {
      id(_packs)
  }
  immutable _packs: [Pack<T>]
}

struct Pack<each T> {}

fn id<T>(_ t: T) -> T {
  return t
}
