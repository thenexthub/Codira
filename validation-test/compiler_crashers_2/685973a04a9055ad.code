// {"signature":"(anonymous namespace)::ExprRewriter::coerceToType(language::Expr*, language::Type, language::constraints::ConstraintLocatorBuilder)"}
// RUN: not --crash %target-language-frontend -typecheck %s
struct a < each b : Collection {
      c : ( repeat each b
        extension
  struct Index {
          d : (repeat(each b.Index
  } var endIndex : Index {
                      Index(d: repeat (each c).endIndex
