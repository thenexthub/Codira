{
    "compilerOptions": {
        "package": "Entry",
        "baseUrl": "${CURRENT_SOURCE_DIR}/entry",
        "cacheDir": "${DEFAULT_CACHE_DIR}",
        "dependencies": {
            "@panda/lib":  { "path": "${LIB_SOURCE_ABC}", "mainFile": "index" }
        }
    }
}
