#===----------------------------------------------------------------------===#
#   Copyright (c) NeXTHub Corporation. All Rights Reserved.
#   DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
#
#   Author: Tunjay Akbarli
#
#   Licensed under the Apache License, Version 2.0 (the "License");
#   you may not use this file except in compliance with the License.
#   You may obtain a copy of the License at:
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
#   Unless required by applicable law or agreed to in writing, software
#   distributed under the License is distributed on an "AS IS" BASIS,
#   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#   See the License for the specific language governing permissions and
#   limitations under the License.
#
#   Please contact NeXTHub Corporation, 651 N Broad St, Suite 201,
#   Middletown, DE 19709, New Castle County, USA.
#===----------------------------------------------------------------------===#

---
cases:
    - doc: Access protected field
      decl: |-
        class C {
          protected count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(c: C): number {
            return c.count
          }
        }
        function main() {
          let c: C = new C(2)
          arktest.assertEQ(C.getCount(c), 2)
        }

    - doc: Access inherited protected field
      decl: |-
        class B {
          protected count: number
          constructor(count: number) {
            this.count = count
          }
        }
        class C extends B {
          constructor(count: number) {
            super(count)
          }
          static getCount(c: C): number {
            return c.count
          }
        }
        function main() {
          let c: C = new C(2)
          arktest.assertEQ(C.getCount(c), 2)
        }

    - doc: Access derived protected field
      decl: |-
        class B {
          protected count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(c: C): number {
            return c.count
          }
        }
        class C extends B {
          constructor(count: number) {
            super(count)
          }
        }
        function main() {
          let b: B = new B(1)
          let c: C = new C(2)
          arktest.assertEQ(B.getCount(c), 2)
        }

    - doc: Access protected field from local variable
      decl: |-
        class C {
          protected count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(i: number): number {
            let c: C = new C(i+1)
            return c.count
          }
        }
        function main() {
          arktest.assertEQ(C.getCount(2), 3)
        }

    - doc: Access derived protected field from local variable
      decl: |-
        class B {
          protected count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(i: number): number {
            let c: C = new C(i+1)
            return c.count
          }
        }
        class C extends B {
          constructor(count: number) {
            super(count)
          }
        }
        function main() {
          arktest.assertEQ(C.getCount(2), 3)
        }

    - doc: Access private field
      decl: |-
        class C {
          private count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(c: C): number {
            return c.count
          }
        }
        function main() {
          let c: C = new C(2)
          arktest.assertEQ(C.getCount(c), 2)
        }

    - doc: Access inherited private field
      tags: compile-only, negative
      decl: |-
        class B {
          private count: number
          constructor(count: number) {
            this.count = count
          }
        }
        class C extends B {
          constructor(count: number) {
            super(count)
          }
          static getCount(c: C): number {
            return c.count
          }
        }

    - doc: Access private field by derived class instance
      decl: |-
        class B {
          private count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(c: C): number {
            return c.count
          }
        }
        class C extends B {
          constructor(count: number) {
            super(count)
          }
        }
        function main() {
          let b: B = new B(1)
          let c: C = new C(2)
          arktest.assertEQ(B.getCount(c), 2)
        }

    - doc: Access private field from local variable
      decl: |-
        class C {
          private count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(i: number): number {
            let c: C = new C(i+1)
            return c.count
          }
        }
        function main() {
          arktest.assertEQ(C.getCount(2), 3)
        }

    - doc: Access derived private field from local variable
      decl: |-
        class B {
          private count: number
          constructor(count: number) {
            this.count = count
          }
          static getCount(i: number): number {
            let c: C = new C(i+1)
            return c.count
          }
        }
        class C extends B {
          constructor(count: number) {
            super(count)
          }
        }
        function main() {
          arktest.assertEQ(C.getCount(2), 3)
        }
