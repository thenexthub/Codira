{
    "compilerOptions": {
        "package": "@mismatched",
        "outDir": "${CMAKE_CURRENT_BINARY_DIR}/modules-build/abc",
        "baseUrl": "."
    },
    "include": [
        "./**/*.ets"
    ]
}
