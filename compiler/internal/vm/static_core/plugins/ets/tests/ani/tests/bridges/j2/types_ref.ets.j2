/*
 * Copyright (c) NeXTHub Corporation. All Rights Reserved.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Author: Tunjay Akbarli
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at:
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * Please contact NeXTHub Corporation, 651 N Broad St, Suite 201,
 * Middletown, DE 19709, New Castle County, USA.
 */

package EtsAniTests

native function checkRefType{{ count }}(
  {% for i in range(count) -%}
  arg{{ loop.index }}: String{% if not loop.last %}, {% endif %}
  {%- endfor %}
): void

@ani.unsafe.Quick
native function checkQuickRefType{{ count }}(
  {% for i in range(count) -%}
  arg{{ loop.index }}: String{% if not loop.last %}, {% endif %}
  {%- endfor %}
): void

native function checkNull{{ count }}(
  {% for i in range(count) -%}
     arg{{ loop.index }}: null{% if not loop.last %}, {% endif %}
  {%- endfor %}
): void

@ani.unsafe.Quick
native function checkQuickNull{{ count }}(
  {% for i in range(count) -%}
     arg{{ loop.index }}: null{% if not loop.last %}, {% endif %}
  {%- endfor %}
): void

native function checkUnderfined{{ count }}(
  {% for i in range(count) -%}
      arg{{ loop.index }}: undefined{% if not loop.last %}, {% endif %}
  {%- endfor %}
): void

@ani.unsafe.Quick
native function checkQuickUnderfined{{ count }}(
  {% for i in range(count) -%}
     arg{{ loop.index }}: undefined{% if not loop.last %}, {% endif %}
  {%- endfor %}
): void

function testRef{{ count }}() {
    {% for i in range(count) -%}
        let str{{ loop.index }}: String = "abcde"
    {% endfor -%}
    checkRefType{{ count }}(
    {%- for i in range(count) -%}
        str{{ loop.index }}{% if not loop.last %}, {% endif %}
    {%- endfor %})
    checkQuickRefType{{ count }}(
    {%- for i in range(count) -%}
        str{{ loop.index }}{% if not loop.last %}, {% endif %}
    {%- endfor %})
}

function testNull{{ count }}() {
    checkNull{{ count }}(
    {%- for i in range(count) -%}
        null{% if not loop.last %}, {% endif %}
    {%- endfor %})
    checkQuickNull{{ count }}(
    {%- for i in range(count) -%}
        null{% if not loop.last %}, {% endif %}
    {%- endfor %})
}

function testUnderfined{{ count }}() {
    checkUnderfined{{ count }}(
    {%- for i in range(count) -%}
        undefined{% if not loop.last %}, {% endif %}
    {%- endfor %})
    checkQuickUnderfined{{ count }}(
    {%- for i in range(count) -%}
        undefined{% if not loop.last %}, {% endif %}
    {%- endfor %})
}

function main() {
    loadLibrary("types_ref_{{ count }}")
    testRef{{ count }}()
    testNull{{ count }}()
    testUnderfined{{ count }}()
}
