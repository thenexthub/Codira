# Copyright (c) 2025-2026 Huawei Device Co., Ltd.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---
cases:
    - decl: |-
          // call another constructor in the same class
          class A {
              fld: string
              constructor() {
                  this('hello')
              }
              constructor(p: string) {
                  this.fld = p
              }
          }
      use: |-
          arktest.assertEQ(new A('abc').fld, 'abc')
          arktest.assertEQ(new A().fld, 'hello')

    - decl: |-
          // call constructors in the same class
          class A {
              fld: string
              constructor() {
                  this(42.0)
              }
              constructor(p: number) {
                  this('' + p)
              }
              constructor(p: string) {
                  this.fld = p
              }
          }
      use: |-
          arktest.assertEQ(new A('abc').fld, 'abc')
          arktest.assertEQ(new A().fld, '42')
          arktest.assertEQ(new A(3.14).fld, '3.14')

    - decl: |-
          // call constructor in superclass
          abstract class A {
              fld: string
              constructor(p: number) {
                  this.fld = '' + p
              }
              constructor(p: boolean) {
                  this(p ? 1.0 : 0.0)
              }
          }
          class B extends A {
              constructor(p: int) {
                  this(p + 1.0)
              }
              constructor(p?: number) {
                  if (p == undefined) {
                      super(false)
                  } else {
                      super(p)
                  }
              }
          }
      use: |-
          arktest.assertEQ(new B().fld, '0')
          arktest.assertEQ(new B(42.0).fld, '42')
          arktest.assertEQ(new B(12345).fld, '12346')

    - decl: |-
          // call constructors in superclasses
          abstract class A {
              fld: string
              constructor(p: string) {
                  this.fld = p
              }
              constructor(p: number) {
                  this('' + p)
              }
          }
          abstract class B extends A {
              constructor(p?: string|number) {
                  if (p == undefined) {
                      super('')
                      return
                  } else if (p instanceof number) {
                      super(++p)
                      return
                  }
                  super(p)
              }
          }
          class C extends B {
              constructor(p: Any) {
                  if (p instanceof string) {
                      super(p)
                  } else if (p instanceof number) {
                      super(p)
                  } else {
                      super()
                  }
              }
          }
      use: |-
          arktest.assertEQ(new C(null).fld, '')
          arktest.assertEQ(new C(undefined).fld, '')
          arktest.assertEQ(new C(true).fld, '')
          arktest.assertEQ(new C('abc').fld, 'abc')
          arktest.assertEQ(new C(42.0).fld, '43')

    - decl: |-
          // call super constructor with optional parameter
          class A {
              p: string|undefined
              constructor(p?: string) {
                  this.p = p
              }
          }
          class B extends A {
              s: string[]
              constructor(...s: string[]) {
                  super()
                  this.s = s
              }
          }
      use: |-
          let b = new B()
          arktest.assertEQ(b.p, undefined)
          arktest.assertEQ(b.s.length, 0)

          b = new B('a')
          arktest.assertEQ(b.p, undefined)
          arktest.assertEQ(b.s.length, 1)
          arktest.assertEQ(b.s[0], 'a')

          b = new B('a', 'b', 'c')
          arktest.assertEQ(b.p, undefined)
          arktest.assertEQ(b.s.length, 3)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')
          arktest.assertEQ(b.s[2], 'c')

    - decl: |-
          // call super constructor with explicit 'undefined' for optional parameter
          class A {
              p: string|undefined
              constructor(p?: string) {
                  this.p = p
              }
          }
          class B extends A {
              s: string[]
              constructor(...s: string[]) {
                  super(undefined)
                  this.s = s
              }
          }
      use: |-
          let b = new B()
          arktest.assertEQ(b.p, undefined)
          arktest.assertEQ(b.s.length, 0)

          b = new B('a')
          arktest.assertEQ(b.p, undefined)
          arktest.assertEQ(b.s.length, 1)
          arktest.assertEQ(b.s[0], 'a')

          b = new B('a', 'b', 'c')
          arktest.assertEQ(b.p, undefined)
          arktest.assertEQ(b.s.length, 3)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')
          arktest.assertEQ(b.s[2], 'c')

    - decl: |-
          // call super constructor with value for optional parameter
          class A {
              p: string|undefined
              constructor(p?: string) {
                  this.p = p
              }
          }
          class B extends A {
              s: string[]
              constructor(...s: string[]) {
                  super('super')
                  this.s = s
              }
          }
      use: |-
          let b = new B()
          arktest.assertEQ(b.p, 'super')
          arktest.assertEQ(b.s.length, 0)

          b = new B('a')
          arktest.assertEQ(b.p, 'super')
          arktest.assertEQ(b.s.length, 1)
          arktest.assertEQ(b.s[0], 'a')

          b = new B('a', 'b', 'c')
          arktest.assertEQ(b.p, 'super')
          arktest.assertEQ(b.s.length, 3)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')
          arktest.assertEQ(b.s[2], 'c')

    - decl: |-
          // call super constructor with rest parameter
          class A {
              s: string[]
              constructor(...s: string[]) {
                  this.s = s
              }
          }
          class B extends A {
              constructor(p1: string, p2: string) {
                  super(p1, p2)
              }
          }
      use: |-
          let b = new B('a', 'b')
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')

    - decl: |-
          // call super constructor with rest parameter
          class A {
              s: string[]
              constructor(...s: string[]) {
                  this.s = s
              }
          }
          class B extends A {
              constructor(p1: string, p2?: string) {
                  super(p1, p2 ?? 'b')
              }
          }
      use: |-
          let b = new B('a')
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')

          b = new B('a', undefined)
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')

          b = new B('a', 'c')
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'c')

    - decl: |-
          // call super constructor with rest parameter
          class A {
              s: string[]
              constructor(...s: string[]) {
                  this.s = s
              }
          }
          class B extends A {
              constructor(p1?: string, p2: string = 'b') {
                  super(p1 ?? 'x', p2)
              }
          }
      use: |-
          let b = new B()
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'x')
          arktest.assertEQ(b.s[1], 'b')

          b = new B('y')
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'y')
          arktest.assertEQ(b.s[1], 'b')

          b = new B('y', 'z')
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'y')
          arktest.assertEQ(b.s[1], 'z')

    - decl: |-
          // call super constructor with spread expression for rest parameter
          class A {
              s: string[]
              constructor(...s: string[]) {
                  this.s = s
              }
          }
          class B extends A {
              constructor(p1: string, p2: string) {
                  super(...[p1, p2])
              }
          }
      use: |-
          let b = new B('a', 'b')
          arktest.assertEQ(b.s.length, 2)
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')

    - decl: |-
          // call super constructor with spread expression for rest parameter
          class A {
              s: string[]
              constructor(...s: string[]) {
                  this.s = s
              }
          }
          class B extends A {
              constructor(...p: string[]) {
                  super(...p)
              }
          }
      use: |-
          let b = new B()
          arktest.assertEQ(b.s.length, 0)

          b = new B('a', 'b', 'c')
          arktest.assertEQ(b.s[0], 'a')
          arktest.assertEQ(b.s[1], 'b')
          arktest.assertEQ(b.s[2], 'c')

    - decl: |-
          // call super constructor with optional parameters
          class A {
              p1: string[]
              p2: string[]
              constructor(p1: string[] = [], p2: string[] = []) {
                  this.p1 = p1
                  this.p2 = p2
              }
          }
          class B extends A {
              constructor(...p: string[]) {
                  super(p)
              }
          }
      use: |-
          let b = new B('a', 'b', 'c')
          arktest.assertEQ(b.p1.length, 3)
          arktest.assertEQ(b.p2.length, 0)

          b = new B()
          arktest.assertEQ(b.p1.length, 0)
          arktest.assertEQ(b.p2.length, 0)

    - decl: |-
          // call super constructor with explicit 'undefined' for optional parameter
          class A {
              p1: string[]
              p2: string[]
              constructor(p1: string[] = [], p2: string[] = []) {
                  this.p1 = p1
                  this.p2 = p2
              }
          }
          class B extends A {
              constructor(...p: string[]) {
                  super(p, undefined)
              }
          }
      use: |-
          let b = new B('a', 'b', 'c')
          arktest.assertEQ(b.p1.length, 3)
          arktest.assertEQ(b.p2.length, 0)

          b = new B()
          arktest.assertEQ(b.p1.length, 0)
          arktest.assertEQ(b.p2.length, 0)

    - decl: |-
          // call super constructor with explicit 'undefined' for optional parameter
          class A {
              p1: string[]
              p2: string[]
              constructor(p1: string[] = [], p2: string[] = []) {
                  this.p1 = p1
                  this.p2 = p2
              }
          }
          class B extends A {
              constructor(...p: string[]) {
                  super(undefined, p)
              }
          }
      use: |-
          let b = new B('a', 'b', 'c')
          arktest.assertEQ(b.p1.length, 0)
          arktest.assertEQ(b.p2.length, 3)

          b = new B()
          arktest.assertEQ(b.p1.length, 0)
          arktest.assertEQ(b.p2.length, 0)
