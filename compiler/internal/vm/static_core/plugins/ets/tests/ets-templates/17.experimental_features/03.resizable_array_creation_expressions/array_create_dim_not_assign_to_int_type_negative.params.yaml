# Copyright (c) 2021-2026 Huawei Device Co., Ltd.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---
cases:
  - use: |-
      let v: number[] = new number[new Boolean(true)] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number[false] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number[new Object()] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number[new Error()] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number[null] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number[undefined] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number["abc"] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - use: |-
      let v: number[] = new number[c'a'] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: int|short = 42 as short
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: BigInt = new BigInt(1n)
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: bigint = 1n
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: Long = new Long(1 as long)
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[] = new number[size](1.0) // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[] = new number[size]((index): number => { return 1.0 }) // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][] = new number[size][1] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][] = new number[1][size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][] = new number[1][size](1.0) // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][] = new number[1][size]((index1, index2): number => { return 1.0 }) // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][][] = new number[size][1][1] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][][] = new number[1][size][1] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][][] = new number[1][1][size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: long = 1
    use: |-
      let v: number[][][] = new number[1][1][size](1.0) // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: Number = new Number(1.0)
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: number = 1.0
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: number = 1.0
    use: |-
      let v: number[][] = new number[1][size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: number = 1.0
    use: |-
      let v: number[][][] = new number[1][1][size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: Float = new Float(1.0f)
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: float = 1.0f
    use: |-
      let v: number[] = new number[size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: float = 1.0f
    use: |-
      let v: number[][] = new number[1][size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      let size: float = 1.0f
    use: |-
      let v: number[][][] = new number[1][1][size] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      // enum constants of string type
      enum Color { Red = 'r', Green = 'g', Blue = 'b' }
    use: |-
      let v: number[] = new number[Color.Green] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      // enum constants of long type
      enum Color { Red = 9223372036854775807, Green = 2, Blue = 3 }
    use: |-
      let v: number[] = new number[Color.Green] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      // enum constants of long type
      enum Color: long { Red = 1, Green = 2, Blue = 3 }
    use: |-
      let v: number[] = new number[Color.Green] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      // enum constants of float type
      enum Color: float { Red = 1.0f, Green = 2.0f, Blue = 3.0f }
    use: |-
      let v: number[] = new number[Color.Green] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"

  - decl: |-
      // enum constants of double type
      enum Color: double { Red = 1.0, Green = 2.0, Blue = 3.0 }
    use: |-
      let v: number[] = new number[Color.Green] // CTE: dimension is not assignable to int type
    expected_out: "Expected CTE output is unknown"
