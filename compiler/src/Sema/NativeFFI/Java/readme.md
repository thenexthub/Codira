# Native FFI with Java

This directory includes semantics logics related to Codira **Native** FFI with Java.

It declares desugaring for mirrors (foreign types), impls (export types), java source code generation, etc.
