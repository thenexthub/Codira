// RUN: %target-languagec_driver -### %s %S/Inputs/libEmpty.so | %FileCheck %s

// REQUIRES: autolink-extract

// CHECK-NOT: language-autolink-extract {{.+}}.o {{.+}}Inputs/libEmpty.so -o {{.+}}.autolink
