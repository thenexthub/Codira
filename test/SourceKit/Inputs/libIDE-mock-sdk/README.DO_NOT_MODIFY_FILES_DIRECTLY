Don't modify files directly here.  Instead, modify the files in the language
repository and copy over the mock SDK like this:

cp -r ../language/test/IDE/Inputs/mock-sdk/*.framework test/Inputs/libIDE-mock-sdk/
