// RUN: %target-language-frontend %s -profile-generate -profile-coverage-mapping -sanitize=address -emit-ir -o - | %FileCheck %s
// RUN: %target-language-frontend -profile-generate -profile-coverage-mapping -emit-ir %s

// CHECK: main
fn main() {}
