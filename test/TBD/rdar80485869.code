// RUN: %empty-directory(%t)
// RUN: %target-language-frontend %s -emit-module-path %t/module -emit-tbd -enable-testing -target %target-language-5.1-abi-triple -tbd-install_name test 

public actor Tom {
    init() async {
    }
}
