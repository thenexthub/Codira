// RUN: not %target-typecheck-verify-language
// RUN: %target-typecheck-verify-language -enable-library-evolution -experimental-lazy-typecheck

// With lazy typechecking enabled and no compiler outputs specified, no errors
// should be emitted for this semantically invalid code.

var x: DoesNotExist
